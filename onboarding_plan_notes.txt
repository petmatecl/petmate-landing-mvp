
// Plan: Add isProfileComplete and missingSteps to UserContext
//
// 1. Define OnboardingSteps enum or type: 'EMAIL_VERIFIED' | 'ROLE_SELECTED' | 'PROFILE_BASIC'
// 2. In calculateValues, determine completeness.
//    - PROFILE_BASIC: checks nombre, surname? (Already enforced by Register, but good to check).
//    - ROLE_SELECTED: checks if profile.roles is not empty or activeRole is valid.
// 3. Expose checkOnboarding() function or similar to easier usage in Guards.
//
// We will then separate RoleGuard into a more generic RouteGuard or keep RoleGuard but add onboarding checks inside it.
// The user request suggests: "requires: ['role:sitter', 'profile:complete']" usage pattern.
// 
// I will create a `Protect.tsx` or `Guard.tsx` component that handles this, 
// OR upgrade `RoleGuard` to accept an array of requirements.
//
// Let's refactor UserContext first.
